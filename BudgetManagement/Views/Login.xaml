<Page x:Class="BudgetManagement.Views.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#EEF2FF">

    <Grid>
        <Border Width="360"
                Padding="28"
                CornerRadius="20"
                Background="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="25"
                                  ShadowDepth="0"
                                  Color="#33000000"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="予算システム"
                   FontSize="30"
                   FontWeight="SemiBold"
                   Foreground="#6366F1"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,4"/>


                <TextBlock Text="ユーザーID"
                           FontSize="13"
                           Foreground="#6B7280"
                           Margin="0,0,0,6"/>

                <Border CornerRadius="10"
                        BorderThickness="1"
                        Background="#F9FAFB">

                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#D1D5DB"/>
                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource FindAncestor, AncestorType=TextBox}}" Value="True">
                                    <Setter Property="BorderBrush" Value="#6366F1"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </DataTrigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#A5B4FC"/>
                                </Trigger>

                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <TextBox BorderThickness="0"
                             Background="Transparent"
                             Padding="12,8"
                             FontSize="15"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                </Border>

                <TextBlock Text="パスワード"
                           FontSize="13"
                           Foreground="#6B7280"
                           Margin="0,16,0,6"/>

                <Border CornerRadius="10"
                        BorderThickness="1"
                        Background="#F9FAFB">

                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#D1D5DB"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource FindAncestor, AncestorType=PasswordBox}}" Value="True">
                                    <Setter Property="BorderBrush" Value="#6366F1"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </DataTrigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="#A5B4FC"/>
                                </Trigger>

                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <PasswordBox x:Name="LoginPassword"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="12,8"
                                 FontSize="15"/>
                </Border>

                <Button Content="ログイン"
                        Margin="0,24,0,0"
                        Height="45"
                        FontWeight="Bold"
                        Foreground="White"
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Click="Login_Click"
                        Command="{Binding LoginCommand}">

                    <Button.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366F1" Offset="0"/>
                            <GradientStop Color="#4F46E5" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btn"
                                    CornerRadius="12"
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btn" Property="Opacity" Value="0.9"/>
                                </Trigger>

                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="btn" Property="Opacity" Value="0.75"/>
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>

                </Button>

            </StackPanel>

        </Border>

        <TextBlock Text="Ver：1.01"
               HorizontalAlignment="Right"
               VerticalAlignment="Bottom"
               Margin="0,0,20,12"
               FontSize="15"
               Foreground="#9CA3AF"/>

    </Grid>
</Page>